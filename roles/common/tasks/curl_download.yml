- name: Download {{ COMPONENT }} component
  get_url:
    url: "{{URL}}"
    dest: /tmp/{{APP}}.zip

- name: Remove old content
  file:
    path: "{{EXTRACT_DIR}}"
    state: absent

- name: Create directory for {{ COMPONENT }} component
  file:
    path: "{{EXTRACT_DIR}}"
    state: directory
  become_user: "{{APP_USER}}"

- name: Unarchive {{ COMPONENT }} Component
  ansible.builtin.unarchive:
    src: /tmp/{{COMPONENT}}.zip
    dest: "{{EXTRACT_DIR}}"
    remote_src: yes
  become_user: "{{APP_USER}}"

- name: Move Htdocs Directory
  shell: mv /usr/share/nginx/html/frontend-main/static/* /usr/share/nginx/html/.
  when: COMPONENT == "frontend"

- name: Move catalogue Directory
  shell: mv /home/roboshop/catalogue/catalogue-main/* /home/roboshop/catalogue/.
  when: COMPONENT == "catalogue"
  become_user: "{{APP_USER}}"

- name: Move user Directory
  shell: mv /home/roboshop/user/user-main/* /home/roboshop/user/.
  when: COMPONENT == "user"
  become_user: "{{APP_USER}}"

- name: Move cart Directory
  shell: mv /home/roboshop/cart/cart-main/* /home/roboshop/cart/.
  when: COMPONENT == "cart"
  become_user: "{{APP_USER}}"
